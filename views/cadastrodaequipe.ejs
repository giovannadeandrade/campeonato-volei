<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Equipe - Campeonato de Vôlei</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>Cadastro de Equipe</h1>
            
            <% if (erros && erros.length > 0) { %>
                <div class="erro-msg">
                    <ul>
                        <% erros.forEach(erro => { %>
                            <li><%= erro %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>
            
            <form method="POST" action="/salvar-equipe">
                <div class="form-group">
                    <label for="nome">Nome da Equipe:</label>
                    <input type="text" id="nome" name="nome" 
                           placeholder="Digite o nome da equipe" 
                           value="<%= dados ? dados.nome || '' : '' %>" 
                           required>
                </div>
                
                <div class="form-group">
                    <label for="tecnico">Técnico Responsável:</label>
                    <input type="text" id="tecnico" name="tecnico" 
                           placeholder="Digite o nome do técnico" 
                           value="<%= dados ? dados.tecnico || '' : '' %>" 
                           required>
                </div>
                
                <div class="form-group">
                    <label for="telefone">Telefone do Técnico:</label>
                    <input type="text" id="telefone" name="telefone" 
                           placeholder="(00) 00000-0000" 
                           value="<%= dados ? dados.telefone || '' : '' %>" 
                           maxlength="15"
                           required>
                    <small>Formato: (00) 00000-0000</small>
                </div>
                
                <div class="nav-links">
                    <button type="submit" class="btn">Salvar Equipe</button>
                    <a href="/menu" class="btn btn-secondary">Voltar ao Menu</a>
                </div>
            </form>
        </div>
    </div>
    
    <script src="/js/mascara-telefone.js"></script>
</body>
</html>
